# Tech Stack
Backend: FastAPI + Uvicorn (Python 3.12+, SQLite stdlib sqlite3 — 2 tables jobs/findings — no ORM), pyyaml, pydantic; engine: scanner/deep-eye git submodule (deep_eye.py CLI, 60+ checks, Python subprocess + ANSI strip, SSE via StreamingResponse); Frontend: Vue 3 Composition API + TypeScript 5.5 + Vite 5 + pnpm 11 + Tailwind 3.4 (SOC Cyberpunk Glassmorphism tokens), Pinia 2.2, Vue Router, ApexCharts + vue3-apexcharts; Testing: pytest 99.2% cov + Vitest 97.76% + Playwright 1.62.1 E2E (Chromium); Tooling: uv venv, vite-plugin-vue, vue-tsc, jsdom, @vitest/coverage-v8; Infra: scripts/dev.sh (concurrent backend+frontend), sonar-project.properties (Quality Gate 80%), docker-compose planned Phase 4, OWASP Juice Shop via npx juice-shop as test target; Repo path: scanner/deep-eye, api/{main,database,routers/*,services/*}, web/src/{views,stores,api,components,constants}