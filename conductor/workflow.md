# Workflow
Phased delivery: Phase 0 Foundation (submodule+venv+CLI verify) ✅, Phase 1 FastAPI Bridge (SSE, config_service masking, report_store, SQLite) ✅, Phase 2 UI via Open Design MCP (6 views: Dashboard/NewScan/ScanLive/Findings/Reports/Settings 7 tabs, check selector 68x15, CompareScans) ✅, Phase 3 CLI Parity (every config.yaml section has UI surface) ✅, Phase 4 Deploy Hardening (docker-compose, auth token, persistence, HTTPS) ⏳. Rules: deep-eye submodule unmodified; API keys server-side .env only (GET /api/config masked); zero assumption — verify from source; no invented CLI flags (recon/secrets/OpenAPI = config.yaml-driven); shortest diff wins; pnpm packageManager; English UI; GitNexus impact+detect_changes before edit/commit; testing gates pytest/vitest/playwright + pnpm build + vue-tsc must green; plans/SPEC.md = scope SSOT (if not in spec don't build); .opencode triage delegates to specialist subagents + vps-shared-memory via subagent.